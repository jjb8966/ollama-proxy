## CLI Proxy API 환경변수 반영 계획

- [completed] `docker-compose.yml` 에 `CLI_PROXY_API_KEYS`, `CLI_PROXY_API_BASE_URL` 전달 추가
- [completed] `docker-compose.yml` 에 `nginx-network` 외부 네트워크 연결 추가 및 `CLI_PROXY_API_BASE_URL` 기본값 보정
- [completed] `src/handlers/chat.py` 의 `cli-proxy-api` 기본 base URL 을 공용 Docker 네트워크 기준으로 수정
- [completed] `README.md` 예시를 새 기본값과 일치하도록 수정
- [completed] 변경 후 재기동 및 런타임 검증
