# 2026-03-17 Plan: `ollama-cloud:minimax-m2.5` tool-call 응답 수정

## 목표

- `ollama-cloud:minimax-m2.5`가 tool 호출을 반환할 때 응답이 중간에 끊기지 않도록 수정
- Ollama 호환 응답에서 `message.tool_calls`를 보존
- OpenAI 호환 프록시에서도 `tools`, `tool_choice`가 유실되지 않도록 수정

## 작업 항목

- [x] `src/routes/openai.py` (Done)
  - `/v1/chat/completions` 프록시 요청에 `tools`, `tool_choice`를 포함

- [x] `src/handlers/response.py` (Done)
  - 스트리밍 청크에서 `delta.tool_calls`를 추출
  - OpenAI tool_calls를 Ollama `message.tool_calls` 형식으로 변환
  - `finish_reason == "tool_calls"`를 정상 종료로 처리
  - 비스트리밍 응답에서도 `message.tool_calls`를 보존

- [x] `src/routes/ollama.py` (Done)
  - Google 전용 스트림 변환기에서도 동일한 tool-call 종료 규칙을 적용
  - 가능하면 Ollama 형식 tool_calls를 함께 전달

- [x] 검증 (Done)
  - `python3 -m py_compile src/handlers/response.py src/routes/openai.py src/routes/ollama.py`
  - 로컬 샘플 payload로 streaming/non-streaming tool-call 변환 결과 확인
  - AST 검사로 OpenAI 프록시 요청에 `tools`, `tool_choice` 키 포함 확인

## 구현 원칙

- 기존 사용자 수정이 있는 파일은 필요한 부분만 최소 변경
- `any`, `unknown` 추가 사용 금지
- 새 동작은 텍스트 응답과 tool-call 응답 모두를 깨지 않도록 하위 호환 유지

## 완료 기준

- tool-call 응답에서 `done: true` final chunk가 생성됨
- Ollama 응답 `message.tool_calls`가 유지됨
- OpenAI 라우트가 `tools`, `tool_choice`를 프록시 payload에 포함함
- 로컬 검증 결과가 기대 형식과 일치함
