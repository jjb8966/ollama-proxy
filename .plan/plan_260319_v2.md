# 구현 계획: 80% 초과 시 compact 응답 전환

**작성일:** 2026-03-19
**관련 연구:** `research_260319_v2.md`

---

## 1. 목표

요청 페이로드가 모델 max context의 80%를 초과하면 원 모델 호출 대신
`antigravity:anti-gemini-3.1-pro-high`를 사용해 compact 요약 응답을 반환합니다.

---

## 2. 작업 항목

### 2.1 compact 상수 정의

- compact 모델 상수 추가
- 사용자 제공 프롬프트 상수 추가
- compact 임계값 비율 `0.8` 추가

**상태:** ✅ Done

### 2.2 요청 페이로드 추정 함수 추가

- `messages`, `tools`, `tool_choice`, `max_tokens`를 포함한 JSON 직렬화
- 근사 토큰 수 계산
- 대상 모델 `context_length`와 비교

**상태:** ✅ Done

### 2.3 compact 요청 생성 함수 추가

- `system`에 compact 프롬프트 삽입
- `user`에 원 요청 payload JSON 삽입
- 내부 재귀 방지 플래그 추가

**상태:** ✅ Done

### 2.4 `ChatHandler` compact 분기 구현

- `handle_chat_request()` 초반에 compact 여부 판단
- 80% 초과 시 compact 요청 결과를 그대로 반환

**상태:** ✅ Done

### 2.5 테스트 추가

- 80% 미만이면 compact 미실행
- 80% 초과이면 compact 실행
- compact 프롬프트가 정확히 전달되는지 검증

**상태:** ✅ Done

### 2.6 검증

- 단위 테스트 실행
- 컴파일 검증

**상태:** ✅ Done

검증 메모:

- `python3 -m unittest tests.test_chat_handler_limits tests.test_provider_error_handling tests.test_anthropic_handler` 통과
- `python3 -m compileall src tests` 통과
